const chai = require("chai")
const assert = chai.assert;
describe("Map Ex 1", function() {
    it("should return an array of doubles")()=>{
        assert.deepEqual(doubleNumbers([1]), )
    }
    assert
    assert
    assert
    assert
});
