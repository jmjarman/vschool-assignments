const numbers = ([2, 5, 100])

function doubleNumbers(arr){
    let doubles = numbers.map(function(x)) {
   return x * 2;
}
}
module.exports                              
                              
                              
const array = ([2, 5, 100]) 

function stringItUp(arr){
 let string = array.map(function( )){
     
 } 
}

function namesOnly(arr){
     return arr.map(obj => obj.name                         
}
 }

