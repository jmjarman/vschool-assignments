var chai = require("chai");
var assert = chai.assert;

var firstThree = require("../firstThree")


describe("firstThree", function() {
    it("should return n copies of the front", function() {
        assert
        assert
        assert
        assert

    })
